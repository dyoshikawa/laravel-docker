FROM nginx:alpine

RUN adduser -D -s /bin/sh -u 1000 dockeruser
RUN sed -i 's/user\ \ nginx\;/user\ \ dockeruser\;/g' /etc/nginx/nginx.conf

ADD ./vhost.conf /etc/nginx/conf.d/default.conf

WORKDIR /etc/nginx
